const BlackButton = ({ text }) => {
  return (
    <button className="bg-gray-800 text-white px-9 py-2.5 cursor-pointer hover:bg-gray-900">
      {text}
    </button>
  );
};

export default BlackButton;
